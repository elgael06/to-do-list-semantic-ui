import {Header} from 'semantic-ui-react';

const TitleApp = () => {

    return ( <header style={{padding:'20px 1px'}}>
    <Header block as='h1'>To-do list</Header>
</header>);
}

export default TitleApp;